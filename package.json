{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "test:unit": "echo 'Legacy browser-only JS unit tests retired with the PWA entrypoint.'",
        "test:e2e": "npx playwright test -c playwright/playwright.config.ts"
    },
    "devDependencies": {
        "@playwright/test": "^1.59.0",
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "fake-indexeddb": "^6.2.5",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
